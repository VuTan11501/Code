# 📚 Project Knowledge Base

> **Living document.** Update it whenever you discover a non-obvious gotcha,
> decide on a convention, or learn a lesson the hard way. **If a bug class
> takes >15 minutes to root-cause, or recurs across sessions, APPEND TO §3.**
> Goal: never debug the same class of bug twice.
>
> See `AGENTS.md` (local) for the full living-document protocol: triggers,
> mandatory entry format (Symptom / Root cause / Fix / Prevention), and
> update rules for other sections.

---

## 1. Repository map

| Path | What lives there | Tech |
|---|---|---|
| `docs/` | Static dashboard (GitHub Pages) — Suica viewer, planner, OT optimizer, DokoKin extension | Vanilla HTML/CSS/JS, Tailwind, shadcn-style tokens |
| `docs/index.html` | Main workflow dashboard | + `docs/js/*.js` modules |
| `docs/suica.html` | Suica history viewer + trip planner | + `docs/js/suica-planner.js`, `docs/js/suica-history.js` |
| `docs/css/style.css` | Shared design-system stylesheet (theme tokens, components) | — |
| `docs/dist/app.bundle.min.js` | Built JS bundle (regenerated by `scripts/build-js.mjs`) | — |
| `.github/skills/suica-history-generator/` | Python skill: generate byte-faithful Mobile Suica PDFs via GitHub Actions | Python + PyMuPDF + IPAGothic |
| `.github/skills/suica-pdf-editor/` | Python skill: parse/edit existing Suica PDFs (`suica_update.py`) | Python + PyMuPDF |
| `.github/skills/dokokin-auto-checkin/` | DokoKin (FJP kintai) auto checkin/checkout via Azure AD | Python |
| `.github/skills/dokokin-azure-login/` | Azure AD OAuth2 PKCE token cache for DokoKin | Python |
| `.github/skills/ot-salary-optimizer/` | OT shift optimizer | Python |
| `.github/skills/rakuraku-suica-expense/` | Auto-file Suica transit expenses into Rakuraku Seisan | Python + Playwright |
| `.github/skills/japanese-ocr/` | Offline Japanese OCR via Tesseract | Python |
| `.github/workflows/*.yml` | GitHub Actions — dispatched from the dashboard | YAML |
| `cloudflare-worker/`, `cloudflare-worker-fare/`, `worker/` | CF Worker proxies (e.g. Suica fare API) | TS |
| `src/` | Next.js premium UI staging (Tans Agents UI) | Next.js 15, shadcn/ui |
| `finance/`, `scripts/`, `tests/` | Helpers, build scripts, integration tests | — |

---

## 2. Commit convention (scope catalog) ⚠️ ENFORCED

Use **Conventional Commits with a feature scope** so we can `git log --grep` and trace work back to a feature instantly.

Format: `<type>(<scope>): <subject>` + optional body + `Co-authored-by:` trailer.

### Allowed types
`feat`, `fix`, `refactor`, `perf`, `test`, `docs`, `chore`, `style`, `revert`, `ci`, `build`.

### Allowed scopes (extend this list when adding a new feature)

| Scope | Touches |
|---|---|
| `suica-pdf` | Suica PDF generator/verify (`scripts/pdf_export.py`, `verify_pdf.py`, `suica_update.py`) |
| `suica-ui` | Suica dashboard page (`docs/suica.html`, suica-only CSS) |
| `suica-planner` | Trip planner JS (`docs/js/suica-planner.js`) |
| `suica-history` | History viewer JS (`docs/js/suica-history.js`) |
| `suica-fare` | Fare proxy / cache (`cloudflare-worker-fare/`) |
| `dokokin` | DokoKin skill + dashboard extension |
| `ot` | OT salary optimizer |
| `rakuraku` | Rakuraku Seisan automation |
| `dashboard` | Main dashboard chrome (`docs/index.html`, theme, layout) |
| `dashboard-ui` | Shared UI components (toasts, modals, spinner, navbar) |
| `i18n` | Language packs |
| `worker` | Cloudflare Workers |
| `ci` | Workflow YAML, build scripts |
| `agents-ui` | Next.js Tans Agents UI (`src/`) |
| `docs` | Markdown documentation only |

### Examples
✅ `fix(suica-pdf): remap U+00AD soft-hyphen in ToUnicode CMap`
✅ `feat(suica-planner): snapshot tags with chip filter`
✅ `fix(dashboard-ui): keep .spinner-ring rotating under reduced motion`
✅ `revert(suica-ui): remove sticky header and calendar preview`
❌ `fix: bug` (no scope, no info)
❌ `fix(ui): spinner` (scope too generic — which page/feature?)
❌ `fix(csp): patch CMap` (scope unrelated to actual change)

### Trailer (always include)
```
Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

---

## 3. Lessons learned / Gotchas 🪤

Each entry: **symptom → root cause → fix → prevention**. Add new ones as you discover them.

### 3.1 PyMuPDF `subset_fonts()` clobbers hyphen to U+00AD on Linux IPAGothic
- **Symptom**: `verify_pdf.py` reports `NO-SOFT-HYPHEN` failures; `suica_update.parse_pdf` returns rows with empty `amount` cells (cascades to `NO-EMPTY-CELLS`). Only on Linux runners, never on local Windows MSGothic builds.
- **Root cause**: After `doc.subset_fonts()`, the ToUnicode CMap's bfchar entry for the hyphen-minus glyph maps to `<00AD>` (SOFT HYPHEN) instead of `<002D>`. The glyph renders fine, but every text extractor reads `\u00ad` for negative amounts.
- **Fix**: `_fix_tounicode_soft_hyphen()` in `pdf_export.py` walks every PDF object after `subset_fonts()`, finds CMap streams (`beginbfchar`/`beginbfrange`), and rewrites `<00AD>` → `<002D>` via `update_stream`. Mobile Suica never legitimately uses U+00AD, so the substitution is safe.
- **Prevention**: Run the full `verify_pdf` test suite on a Linux container before merging anything that touches `pdf_export.py`.

### 3.2 Global `prefers-reduced-motion` rule kills loading spinners
- **Symptom**: `.spinner-ring` doesn't rotate on Windows/macOS when user has "reduce motion" enabled (very common default on corporate machines).
- **Root cause**: `docs/css/style.css` has a global `@media (prefers-reduced-motion: reduce)` rule that sets `* { animation-duration: 0.01ms !important }`, freezing every animation including loading indicators.
- **Fix**: Loading spinners need their own `!important` animation declaration + a dedicated reduced-motion override that slows them down rather than disabling them (e.g. 1.6s instead of 0.8s).
- **Prevention**: When adding a new animation that **must** still play under reduce-motion (loading, status indicators), declare it with `!important` and add a dedicated slow-motion override — don't rely on the global rule being permissive.

### 3.3 Cache busters are required on every CSS/JS edit
- **Symptom**: User reports "I fixed it but the browser still shows the old version."
- **Root cause**: GitHub Pages caches static assets aggressively. Without bumping `?v=N`, browsers serve stale CSS/JS.
- **Fix**: Bump the `?v=N` query string on **every** `<link>`/`<script>` referencing the edited file. Check both `docs/index.html` and `docs/suica.html`.
- **Prevention**: Always grep `?v=` for the file you edited and bump it as part of the same commit.

### 3.4 `subset_fonts()` is non-negotiable (file size)
- **Symptom**: Output Suica PDF balloons from ~150 KB to 4-5 MB.
- **Root cause**: PyMuPDF embeds the full IPAGothic (~5 MB) on every `insert_text` call.
- **Fix**: Call `doc.subset_fonts()` before save. Real Mobile Suica PDFs ship subsetted fonts.
- **Prevention**: Don't bypass subset_fonts even when it causes other bugs (like §3.1) — patch the side effect instead.

### 3.5 GitHub Actions workflow polling can match the wrong run
- **Symptom**: "Generating Suica PDF" stays in loading state forever, then times out.
- **Root cause**: `_waitForRun` in `suica-planner.js` previously matched on workflow name alone, sometimes binding to an older run from a previous dispatch.
- **Fix**: Capture `prevLatestId` **before** dispatch; after dispatch only accept runs with `id > prevLatestId` AND `created_at` after dispatch time. Surface 401/403/422/rate-limit errors immediately instead of retrying.
- **Prevention**: When polling for a freshly-created resource, always snapshot the "before" state and compare against it; never just look at "latest".

### 3.6 Soft-hyphen normalisation in parsers
- **Symptom**: Anywhere we re-parse text extracted from a Suica PDF, negative amounts may come through as `\u00ad…` even after §3.1 fix (older PDFs in the wild).
- **Fix**: `_parse_money` in `verify_pdf.py` already normalises `\u00ad` → `-`. Any new parser must do the same.
- **Prevention**: Whenever you write a new "parse number from PDF text" function, normalise SHY before regex.

### 3.7 SPEND-TARGET is informational only, never fails the build
- **Symptom**: Verification fails with `[SPEND-TARGET] actual spend ¥X drifts ¥Y from target ¥Z` even though PDF looks fine.
- **Root cause**: The "spend target" is a planning *hint* (user nudges generator toward a budget), not a generator invariant. Route pools have discrete fares and a mandatory commute baseline, so the generator often can't land within ±¥1,000 of any arbitrary target. Older asymmetric tolerance still failed on big over-spend cases (e.g. target ¥13,000 but commute pool already costs ¥26,400).
- **Fix**: `verify_pdf._check_spend_target` always passes; just reports the delta as informational. If the user really cares about budget, they tighten the planner UI inputs, not the verifier.
- **Prevention**: Don't gate CI on planner-quality heuristics. Verifier = byte/format/arithmetic invariants only. Soft targets belong in the planner, not the verifier.

### 3.8 DokoKin `TotalOfBreakTime` is decimal hours, not minutes
- **Symptom**: Checkout shows `60:00` break instead of `01:00`.
- **Root cause**: `TotalOfBreakTime` is sent as decimal **hours** (`1.0`), but worker treated it as minutes and posted `60`.
- **Fix**: `.github/scripts/gh_checkin.py` → `do_dakoku()`: send `break_hours` as `float`; keep shift-threshold logic in hours (`1.0`, `0.75`, `0.0`).
- **Prevention**: Always verify API field units against the Flutter app source (or a successful real session payload) before mirroring.
_(source: checkpoint 003)_

### 3.9 DokoKin `WorkingType` is backend-derived from `(CheckinType, CheckoutType, IsCheckinDakoku)`
- **Symptom**: Timesheet "Working Place" column stays blank even though workplace is set in profile.
- **Root cause**: Backend derives `WorkingType` from a truth table over checkin/checkout type pairs PLUS the counter-intuitive rule `IsCheckinDakoku = (lat==0 && lon==0)`. Mismatched type/GPS combos fall to `-1`.
- **Fix**: Payload builder in `.github/scripts/gh_checkin.py` must match the backend derivation table exactly (or intentionally force a known-good type pair).
- **Prevention**: Treat backend-derived columns as **computed** state; don't assume posting a profile field will populate them.
_(source: checkpoint 004)_

### 3.10 GitHub Actions `cron` can be silently skipped for hours
- **Symptom**: Checkout scheduled for `03:30 JST` fires at `04:33`; sometimes skipped entirely.
- **Root cause**: GitHub Actions cron is best-effort. Both dispatcher and watchdog crons can be skipped; a single self-loop restart hides the gap but extends drift.
- **Fix**: `.github/workflows/scheduled-dispatch.yml` + `dispatcher-watchdog.yml` use multi-offset cron, short self-loop, and an external (off-GitHub) heartbeat fallback.
- **Prevention**: Never rely on a single cron source for time-sensitive automation. Always have at least one off-GitHub heartbeat.
_(source: checkpoints 004, 005)_

### 3.11 Suica parser returns chrome rows mixed with data rows
- **Symptom**: Generated PDF loses header/footer/page-number chrome; only row data survives.
- **Root cause**: `suica_update.parse_pdf()` returns **all** extracted text rows including chrome (page headers, footers). Naive mutators rewrote chrome too.
- **Fix**: `.github/skills/suica-history-generator/scripts/pdf_export.py` → `_is_data_row()` filter; only data rows go through `render()` mutation.
- **Prevention**: Never feed a parser's "all extracted rows" directly into a mutator without a chrome-vs-data discriminator.
_(source: checkpoint 014)_

### 3.12 Mobile Suica truncates station names to 6 characters
- **Symptom**: Long station names (`スポーツセンター`) overflow into the next column on generated PDFs.
- **Root cause**: The official Mobile Suica app truncates station text at 6 chars (preserving trailing `入`/`出` direction marker). Our renderer wrote the full string.
- **Fix**: `.github/skills/suica-history-generator/scripts/pdf_export.py` → `_truncate_station()` caps text, preserving direction marker.
- **Prevention**: When mirroring an existing UI, encode its **truncation/abbreviation rules**, not just its character set.
_(source: checkpoint 014)_

### 3.13 Adding event types breaks chronological ordering invariants
- **Symptom**: Validator rejects generated history; `物販` (shopping) events appear inside trip windows.
- **Root cause**: New event types (shopping, top-up) were inserted without re-verifying ordering constraints against existing trip events. Shopping inside trips, missing AUTO top-up before SHOPPING when balance was low.
- **Fix**: `.github/skills/suica-history-generator/scripts/tap_builder.py` → `_generate_shopping_events()` keeps shopping outside trip windows; `flush_shopping_up_to()` emits AUTO top-up before SHOPPING when needed.
- **Prevention**: Adding a new event class? Enumerate every existing class and verify ordering rules with each pair.
_(source: checkpoint 014)_

### 3.14 `log-viewer.js` re-render must not toggle step open/close state
- **Symptom**: Per-step logs collapse/re-open inconsistently while polling.
- **Root cause**: `loadRunJobs()` mixed state restoration (`openKeys`) with toggle side-effects in the same render pass.
- **Fix**: `docs/js/log-viewer.js` → `loadRunJobs()` simplifies re-render: refresh content only; never call toggle helpers during re-render.
- **Prevention**: Don't mix state **restoration** with toggle **side-effects** in the same render pass. Render = idempotent function of state.
_(source: checkpoint 014)_

### 3.15 `TapEntry.at` not `.datetime`
- **Symptom**: Verifier or generator crashes with `AttributeError: 'TapEntry' object has no attribute 'datetime'`.
- **Root cause**: The dataclass field is named `at`. Code copy-pasted from elsewhere assumed `datetime`.
- **Fix**: `.github/skills/suica-history-generator/scripts/models.py` is source of truth — all readers use `TapEntry.at`.
- **Prevention**: Before wiring new logic against a dataclass, open the model file. Don't infer field names from the domain noun.
_(source: checkpoint 015)_

### 3.16 Real Mobile Suica PDF has **4 station/kind columns**, not 3
- **Symptom**: Generated PDF showed `入 | 横浜出 | (blank) | 東京` — the `出` marker stuffed into the entry station cell, the kind-out column blank. Also `物販 モバイル` rendered the merchant label into the station column when real PDF leaves both station cells empty for shopping.
- **Root cause**: `_collapse_in_out` schema only had `type`/`st_from`/`st_to`. To match real layout (`月 日 種別1 利用駅1 種別2 利用駅2 残高 入金`), `出` was concatenated as suffix to `st_from`, and `SHOPPING.st_from = e.station` printed the internal merchant label `"モバイル"`.
- **Fix**:
  - Add `COL_X["T2"] = 326.44` (probed from `fixtures/template.pdf` via `page.get_text('dict')` looking for `出` X-coord).
  - Add `type_out` field to row dicts; `"出"` for IN→OUT collapsed, `""` elsewhere.
  - `st_from` stores raw station name (no `出` suffix).
  - SHOPPING rows: `st_from = ""` (merchant label is internal-only).
  - Renderer writes `type_out` at `COL_X["T2"]`. Existing SF redact span (263–375) already covers x=326.44, no new redact needed.
- **Prevention**: When mirroring a real document layout, probe column header coordinates with `page.get_text('dict')` BEFORE coding the renderer. Don't assume "type then 2 stations" — Mobile Suica + JR-style statements use repeating `(種別, 利用駅)` pairs.

### 3.17 Template header `残高履歴 （101件）` is baked-in
- **Symptom**: Generated PDF always shows `(101件)` regardless of actual row count.
- **Root cause**: Text is part of the template PDF, not generated. Generator only rewrites data rows.
- **Fix**: (not yet) — would need redact + reinsert at template y=~125 with actual count from `len(rows)`.
- **Prevention**: When auditing template fidelity, list every chrome string and decide which are dynamic. Hard-coded counts are an easy tell of fake PDFs.

---

## 4. Build & test commands

```powershell
# JS bundle (rebuild after any docs/js/*.js edit)
node scripts/build-js.mjs

# Quick syntax check
node --check docs/js/suica-planner.js

# Suica PDF skill — full verify (requires Linux + IPAGothic for full fidelity)
cd .github/skills/suica-history-generator
python -m scripts.verify_pdf out.pdf --template fixtures/template.pdf --target 30000

# Suica PDF skill — pytest suite
pytest .github/skills/suica-history-generator/scripts/tests/ -q

# DokoKin smoke
python .github/skills/dokokin-auto-checkin/auto_checkin.py --status

# Lint / format (if present)
# (no global linter set up — add per-package as needed)
```

---

## 5. Development workflow

1. **Read** `plan.md` in the active session folder before resuming.
2. **Decompose** non-trivial work into todos (SQL `todos` table) with descriptive IDs.
3. **Implement** — small edits inline; larger work via sub-agents per `AGENTS.md`.
4. **Verify** — `node --check` for JS, pytest for Python skills, syntax-check Python via `python -c "import ast; ast.parse(...)"`.
5. **Bump cache buster** if you touched anything under `docs/css/` or `docs/js/`.
6. **Commit** with proper `type(scope): subject` (see §2) + `Co-authored-by:` trailer.
7. **Push** — never leave fixes uncommitted; user shouldn't have to ask "push code lên chưa?".
8. **Update this file** if you discovered a new gotcha worth remembering.

---

## 6. Where to look first

| Problem | Start here |
|---|---|
| Suica PDF output wrong / fails verify | §3.1, §3.4, `.github/skills/suica-history-generator/docs/VERIFY_PDF.md` |
| Suica planner UI bug | `docs/js/suica-planner.js` + `docs/suica.html` |
| Dashboard chrome / theme | `docs/css/style.css` + `docs/js/theme.js` |
| Workflow polling / dispatch | `_dispatchWorkflow` / `_waitForRun` in `suica-planner.js` (§3.5) |
| DokoKin auth | `.github/skills/dokokin-azure-login/SKILL.md` |
| Cache not refreshing | §3.3 |
| Animation stops on some machines | §3.2 |

---

## 7. Per-feature deep-dive docs

- Suica PDF verify checks: `.github/skills/suica-history-generator/docs/VERIFY_PDF.md`
- Suica PDF benchmarks: `.github/skills/suica-history-generator/docs/BENCHMARK.md`
- DokoKin schedule: `.github/skills/dokokin-auto-checkin/may2026-schedule.md`
- Rakuraku selectors: `.github/skills/rakuraku-suica-expense/references/dom-selectors.md`
- Manager + sub-agents workflow: `AGENTS.md`

---

_Last reviewed: 2026-05-23_
